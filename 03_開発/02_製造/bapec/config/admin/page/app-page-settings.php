<?php
/*
|--------------------------------------------------------------------------
| 管理サイト画面デザインの共通定義を設定する。
|--------------------------------------------------------------------------
|
| CoreUI設定
| Formヘルパーを用いてhtmlの要素を作成する場合、共通のclass属性等を設定する。
*/
require(config_path('admin/page/support-parts-date-flatpickr.php'));
require(config_path('admin/page/html.php'));
$adminPageSettings = [
    /*
    |--------------------------------------------------------------------------
    | sidebarの状態定義
    |--------------------------------------------------------------------------
    | c-sidebar-show: 開いた状態
    | ''            : 閉じた状態
    */
    'sidebar' => [
        'c-sidebar' => ''
    ],
    /*
    |--------------------------------------------------------------------------
    | 改ページの定義
    |--------------------------------------------------------------------------
    | [
    |   'display-count' => []
    | ]    : 使用する。
    | false: 使用しない
    */
    'pagination' => [
        /*
        |--------------------------------------------------------------------------
        | ページ内に表示する件数を定義する。
        |--------------------------------------------------------------------------
        */
        'display-count' => [
            'selects' => [
                '20'  => 20,
                '50'  => 50,
                '100' => 100,
            ],
            'default' => 20,
        ],
    ],
    /*
    |--------------------------------------------------------------------------
    | 入力チェック定義
    |--------------------------------------------------------------------------
    */
    'validation' => [
        /*
        |--------------------------------------------------------------------------
        | 入力チェックのタイミング定義
        |--------------------------------------------------------------------------
        | input要素のイベントを半角スペース区切りで定義する。
        | 例： input change blur
        */
        'validation-timing' => 'keyup focusout change',
        /*
        |--------------------------------------------------------------------------
        | エラー通知方式定義
        |--------------------------------------------------------------------------
        | wrapper: 入力欄の下部にエラーを表示する
        | tooltip: ツールチップで表示する
        */
        'error-notify' => 'wrapper',
        /*
        |--------------------------------------------------------------------------
        | エラー通知要素のwrapperタグ
        | error-notify = 'wrapper'の場合、有効
        |--------------------------------------------------------------------------
        | false: 未使用
        */
        'notify-wrapper' => '<ul class="help-block"></ul>'
    ],
    /*
    |--------------------------------------------------------------------------
    | 入力支援部品定義
    |--------------------------------------------------------------------------
    */
    'support-parts' => [
        /*
        |--------------------------------------------------------------------------
        | 日付入力支援部品を定義
        |--------------------------------------------------------------------------
        | array: 使用するflatpicker定義
        | false: 未設定の場合、使用しない
        */
        'date' => $flatpickr,
    ],
    /*
     |--------------------------------------------------------------------------
     | htmlの属性定義
     |--------------------------------------------------------------------------
     */
    'html' => $html
];
