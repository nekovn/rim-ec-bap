<?php
/*
|--------------------------------------------------------------------------
| 会員サイト画面デザインの共通定義を設定する。
|--------------------------------------------------------------------------
| Formヘルパーを用いてhtmlの要素を作成する場合、共通のclass属性等を設定する。
*/
require(config_path('base/page/support-parts-date-flatpickr.php'));
require(config_path('base/page/support-parts-date-fullcalendar.php'));
require(config_path('base/page/html.php'));
$pageSettings = [
    /*
    |--------------------------------------------------------------------------
    | 検索条件値の設定を行う
    |--------------------------------------------------------------------------
    | 0以下の場合、全件取得する。
    */
    'search-limit' => 0,
    /*
    |--------------------------------------------------------------------------
    | 改ページの定義
    |--------------------------------------------------------------------------
    | [
    |   'display-count' => []
    | ]    : 使用する。
    | false: 使用しない
    */
    'pagination' => [
        /*
        |--------------------------------------------------------------------------
        | ページ内に表示する件数を定義する。
        |--------------------------------------------------------------------------
        */
        'display-count' => [
            'selects' => [
                '20'  => 20,
                '50'  => 50,
                '100' => 100,
            ],
            'default' => 20,
        ],
    ],
    /*
    |--------------------------------------------------------------------------
    | 入力チェック定義
    |--------------------------------------------------------------------------
    */
    'validation' => [
        /*
        |--------------------------------------------------------------------------
        | 入力チェックのタイミング定義
        |--------------------------------------------------------------------------
        | input要素のイベントを半角スペース区切りで定義する。
        | 例： input change blur
        */
        'validation-timing' => '',
        /*
        |--------------------------------------------------------------------------
        | エラー通知方式定義
        |--------------------------------------------------------------------------
        | wrapper: 入力欄の下部にエラーを表示する
        | tooltip: ツールチップで表示する
        */
        'error-notify' => 'wrapper',
        /*
        |--------------------------------------------------------------------------
        | エラー通知要素のwrapperタグ
        | error-notify = 'wrapper'の場合、有効
        |--------------------------------------------------------------------------
        */
        'notify-wrapper' => '<ul class="help-block"></ul>'
    ],
    /*
    |--------------------------------------------------------------------------
    | 入力支援部品定義
    |--------------------------------------------------------------------------
    */
    'support-parts' => [
        /*
        |--------------------------------------------------------------------------
        | 住所関連の設定を行う
        |--------------------------------------------------------------------------
        */
        'postcode' => [
            /*
            |--------------------------------------------------------------------------
            | 郵便番号をハイフン区切りするか
            |--------------------------------------------------------------------------
            | true : ハイフン区切り(3桁-4桁)
            | false: 区切らない
            */
            'format-hypen' => false,
        ],
        /*
        |--------------------------------------------------------------------------
        | 電話番号関連の設定を行う
        |--------------------------------------------------------------------------
        */
        'tel' => [
            /*
            |--------------------------------------------------------------------------
            | 電話番号をハイフン区切りするか
            |--------------------------------------------------------------------------
            | true : ハイフン区切り
            | false: 区切らない
            */
            'format-hypen' => true,
        ],
        /*
        |--------------------------------------------------------------------------
        | 日付入力支援部品を定義
        |--------------------------------------------------------------------------
        | array: 使用するflatpicker定義
        | false: 未設定の場合、使用しない
        */
        'date' => $flatpickr,
        /*
        |--------------------------------------------------------------------------
        | カレンダー入力支援部品を定義
        |--------------------------------------------------------------------------
        | array: 使用するfullcalendar定義
        | false: 未設定の場合、使用しない
        */
        'calendar' => $fullcalendar,
    ],
    /*
    |--------------------------------------------------------------------------
    | htmlの属性定義
    |--------------------------------------------------------------------------
    */
    'html' => $html
];
